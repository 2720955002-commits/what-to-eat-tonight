# 「今晚吃啥」产品需求文档 (PRD)

> 版本：v1.0 | 日期：2026-06-06 | 状态：初稿

---

## 1. 项目概述

### 1.1 项目背景
每天面临"今晚吃什么"的灵魂拷问，需要一个工具来帮助快速决策。通过随机抽选 + 个性化配置的方式，解决选择困难症，同时提供菜谱管理、收藏、历史记录等配套功能。

### 1.2 项目目标
打造一款轻量、美观、实用的"晚餐决策助手"，让用户从"不知道吃什么"到"一键决定吃什么"。

### 1.3 项目定位
个人晚餐决策工具 → 未来扩展为家庭/多人用餐管理平台。

### 1.4 核心理念
- **省心**：一键抽选，不用纠结
- **个性化**：加权因素可配置，适配每个人
- **本地优先**：数据存本地，隐私安全，后期可选云同步
- **渐进增强**：MVP先跑通，后续迭代精细化

---

## 2. 目标用户

### 2.1 当前用户画像
- **个人用户**（主要是开发者自己）
- 江西人，口味包容度高
- 日常需要快速决定晚餐吃什么
- 有一定烹饪基础，有自己会做的菜
- 对UI品质有要求（偏好iOS原生感）

### 2.2 扩展用户画像（未来）
- 情侣/家庭用户
- 多设备用户（需云同步）

---

## 3. 技术方案

### 3.1 技术栈

| 层级 | MVP（第一版） | 终态 |
|------|--------------|------|
| 框架 | React + TypeScript | React Native + Expo |
| 样式 | TailwindCSS | TailwindCSS + NativeWind |
| 数据 | IndexedDB（via Dexie.js） | IndexedDB + 云端同步 |
| 构建 | Vite | Expo build |
| 部署 | Vercel（免费） | Vercel + App Store / Google Play |
| 包管理 | pnpm | pnpm |

### 3.2 数据存储策略
- **MVP阶段**：全量数据存储在浏览器 IndexedDB
- **数据模型预留字段**：每个数据实体预留 `id`（UUID）、`createdAt`、`updatedAt`、`syncedAt`，为后续云同步做准备
- **后期演进**：用户体系 → 云端存储 → 多端同步

### 3.3 部署方案
- 源码托管：GitHub
- 自动部署：Vercel（关联 GitHub 仓库，push 即部署）
- 域名：`.vercel.app` 免费域名 / 后续绑定自定义域名

---

## 4. 功能需求（MVP）

### 4.1 首页 — 今晚吃什么（抽选）

#### 4.1.1 核心功能
- **一键抽选**：点击按钮，从菜谱库中随机抽取指定数量的菜
- **设定菜数**：用户可设置今天吃几个菜（1-5个，默认2个）
- **抽选配置入口**：首页提供配置入口，可设置抽选加权因素
- **结果展示**：卡片形式展示抽选结果（菜名、主图缩略图、用时、难度）
- **重新抽选**：不满意可一键重新抽选
- **确定选择**：确认后自动记录到历史

#### 4.1.2 加权随机算法（用户可配置）
支持以下加权因素，用户可自由开启/关闭：

| 因素 | 说明 | 默认状态 |
|------|------|---------|
| 防重复推荐 | 最近N天内吃过的菜权重降低 | 开启（默认7天） |
| 收藏优先 | 收藏的菜权重提高 | 关闭 |
| 口味偏好 | 用户选定的口味标签权重提高 | 关闭 |
| 用时限制 | 仅在指定时长内的菜中抽选 | 关闭（默认30分钟） |
| 冰箱食材 | 根据冰箱里现有的食材，只抽选能做的菜 | 关闭 |
| 包含主食 | 勾选后主食类菜谱进入抽选池，不勾选则排除 | 开启 |

**分类权重调节**（固定规则，不可配置）：
| 分类 | 权重系数 | 说明 |
|------|---------|------|
| 热菜（家常/硬菜） | 1.0 | 正常权重 |
| 汤类 | 1.0 | 正常权重 |
| 凉菜 | 0.5 | 权重降低，避免凑数 |
| 配菜 | 0.5 | 权重降低，避免凑数 |
| 主食 | 1.0 | 正常权重（如「包含主食」开关开启） |

**冰箱食材交互流程**：
1. 用户点选"冰箱有啥菜"开关后，弹出食材选择器
2. 食材选择器提供预设分类列表 + 自定义输入框
3. 用户勾选冰箱里现有的食材
4. 系统从菜谱库中筛选出能用这些食材做的菜，再进行加权随机抽选
5. 如无法组合出足够多的菜，提示用户补充食材

#### 4.1.3 页面元素
- 标题："今晚吃啥"（暖色大字）
- 抽选按钮（大圆角、渐变背景）
- 菜数选择器（+-按钮或滑动选择）
- 配置入口（齿轮图标）
- 抽选结果区域（卡片列表）
- 空状态引导文案

### 4.2 菜谱库

#### 4.2.1 核心功能
- **内置菜谱**：50+ 家常菜，覆盖常见品类
- **菜谱列表**：网格/列表视图展示，每项显示菜名、主图、用时、难度
- **菜谱详情**：图文详情页，包含菜名、主图、食材清单、步骤、用时、口味标签、难度、份数、季节标签
- **用户自定义菜谱**：支持用户添加自己的拿手菜
- **编辑/删除**：用户可编辑或删除自己添加的菜谱（内置菜谱仅可编辑，不可删除）

#### 4.2.2 菜谱数据结构

| 字段 | 类型 | 说明 | 必填 |
|------|------|------|------|
| id | UUID | 唯一标识 | ✅ |
| name | string | 菜名 | ✅ |
| image | string | 主图URL | ✅ |
| ingredients | string[] | 食材列表 | ✅ |
| steps | string[] | 步骤（有序） | ✅ |
| cookTime | number | 用时（分钟） | ✅ |
| difficulty | enum | 简单 / 中等 / 困难 | ✅ |
| servings | number | 适合几人份 | ✅ |
| flavors | string[] | 口味标签（辣/清淡/酸甜等） | ✅ |
| season | string[] | 季节标签（春/夏/秋/冬/四季） | ✅ |
| category | string | 分类（家常菜/硬菜/汤/凉菜等） | ✅ |
| isBuiltIn | boolean | 是否内置菜谱 | ✅ |
| createdAt | timestamp | 创建时间 | ✅ |
| updatedAt | timestamp | 更新时间 | ✅ |
| syncedAt | timestamp | 同步时间（预留） | ❌ |

#### 4.2.3 菜谱难度分布
- 70% 家常菜（简单/中等，30分钟内可完成）
- 30% 硬菜（中等/困难，需较长时间或较高技巧）

#### 4.2.4 地域扩展（后期）
- 后续可上线地域菜谱库切换（赣菜、川菜、粤菜等）
- 用户可选择加载不同地域的菜谱包

### 4.3 筛选

#### 4.3.1 核心功能
- **双模式筛选**：

| 模式 | 场景 | 交互 |
|------|------|------|
| "我有XX食材"模式 | 打开冰箱看到食材，搜能做什么菜 | 选择/输入食材 → 列出匹配菜谱 |
| "我想吃XX"模式 | 今晚想吃某个口味/类型的菜 | 按口味/用时/难度筛选 |

- **组合筛选**：两种模式支持叠加条件
- **搜索结果展示**：筛选结果以列表/网格展示，点击进入详情

#### 4.3.2 筛选条件
- 食材（多选 + 自定义输入）
- 口味（辣 / 清淡 / 酸甜 / 鲜香 / 酱香 / 蒜香）
- 用时（自定义范围，如 15分钟内 / 30分钟内 / 1小时内）
- 难度（简单 / 中等 / 困难）
- 季节（春 / 夏 / 秋 / 冬 / 四季）
- 分类（家常菜 / 硬菜 / 汤 / 凉菜 / 主食）

### 4.4 收藏

#### 4.4.1 核心功能
- **收藏/取消收藏**：在菜谱详情页和抽选结果页可一键收藏
- **收藏列表**：独立的收藏页面，展示所有收藏的菜谱
- **防重复推荐联动**：收藏的菜在抽选时可根据用户配置获得权重加成

#### 4.4.2 数据结构
| 字段 | 类型 | 说明 |
|------|------|------|
| recipeId | UUID | 关联菜谱ID |
| addedAt | timestamp | 收藏时间 |

### 4.5 历史记录

#### 4.5.1 核心功能
- **自动记录**：每次确认抽选结果后自动记录
- **今日记录**：首页/历史页展示"今天吃了啥"
- **按日期分组**：历史记录按日期倒序排列
- **查看详情**：点击历史条目可查看当天选的菜详情
- **清空历史**：支持一键清空

#### 4.5.2 数据结构
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 唯一标识 |
| recipes | UUID[] | 当天选择的菜谱ID列表 |
| date | string | 日期（YYYY-MM-DD） |
| dinnerCount | number | 当天做了几个菜 |
| createdAt | timestamp | 创建时间 |

### 4.6 设置页面

#### 4.6.1 功能列表
- **抽选配置**：加权因素开关 + 参数设置（如防重复天数、用时上限、含主食等）
- **常备食材预设**：设置冰箱常备食材清单，抽选时快捷引用
- **数据管理**：导出数据 / 导入数据 / 清空所有数据
- **关于**：版本号、项目介绍

---

## 5. UI/UX 设计规范

### 5.1 风格定位
- **主调性**：iOS 原生感，干净、简洁、有质感
- **核心元素**：大圆角卡片、大圆角按钮
- **配色**：低饱和度暖色系，避免高饱和度颜色
- **层次感**：适当的颜色渐变，避免纯平涂

### 5.2 配色方案（初版）

| 用途 | 色值（参考） | 说明 |
|------|-------------|------|
| 主色 | `#E8A87C`（暖杏色） | 按钮、强调元素 |
| 主色渐变 | `#E8A87C` → `#D4956B` | 按钮渐变背景 |
| 背景色 | `#FDF8F5`（暖白） | 页面背景 |
| 卡片色 | `#FFFFFF` | 卡片/容器背景 |
| 文字主色 | `#2D2D2D` | 标题/正文 |
| 文字辅色 | `#8B8B8B` | 辅助信息 |
| 分割线 | `#F0EBE6` | 分隔线 |
| 收藏/强调 | `#E87461`（暖珊瑚） | 收藏图标/强调色 |

> 注：以上色值为初版参考，高保真设计时可能微调。

### 5.3 字体
- 系统字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`
- 标题：Semibold, 较大字重
- 正文：Regular

### 5.4 圆角规范
- 卡片：16px
- 按钮：12px
- 输入框：10px
- 图标容器：8px

### 5.5 交互规范
- 按钮点击有微反馈（opacity 变化或轻微缩放）
- 页面切换使用平滑过渡
- 加载态使用骨架屏或微动效
- 空状态有插图和引导文案
- 底部 Tab 导航（5个模块）

---

## 6. 信息架构（页面结构）

```
底部Tab导航（5个Tab）
├── 📌 首页（抽选）
│   ├── 抽选主界面
│   ├── 抽选结果展示
│   └── 冰箱食材选择器（弹窗）
├── 📖 菜谱库
│   ├── 菜谱列表（网格/列表）
│   ├── 菜谱详情页
│   └── 新增菜谱表单
├── 🔍 筛选
│   ├── 筛选条件面板
│   └── 筛选结果列表
├── ❤️ 收藏
│   └── 收藏列表
└── 📋 历史
    └── 历史记录列表（按日期分组）

设置页面（从首页齿轮入口进入）
├── 抽选配置
├── 常备食材预设
└── 数据管理 / 关于
```

---

## 7. 路线图

### Phase 0 — 需求确认 ✅
### Phase 1 — 需求文档 & 设计方案（当前）
### Phase 2 — 技术基建（项目初始化、数据层、部署配置）
### Phase 3 — 核心功能开发（5个模块）
### Phase 4 — 设置 & 配置
### Phase 5 — UI 打磨 & 发布上线
### Phase 6 — 后期演进
- React Native + Expo 多端迁移
- 用户体系 + 云同步
- 地域菜谱库切换
- 智能推荐升级

---

## 8. 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 首页加载 < 1秒，抽选响应 < 200ms |
| 离线可用 | 全量数据本地存储，完全离线可用 |
| 响应式 | Web版在手机浏览器上体验良好 |
| 数据安全 | 本地数据不外传，无后端无追踪 |
| 可扩展 | 数据模型预留云同步字段，架构支持多端迁移 |